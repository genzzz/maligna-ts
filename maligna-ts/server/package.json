{
  "name": "maligna-ts-server",
  "version": "1.0.0",
  "description": "HTTP server for testing the maligna-ts sentence alignment library",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "npm run build && npm run start",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "maligna-ts": "file:.."
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
